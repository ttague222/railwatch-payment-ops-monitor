{
  "buildCommand": "cd railwatch && npm install && npm run build",
  "outputDirectory": "railwatch/dist",
  "installCommand": "echo 'install handled in buildCommand'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
